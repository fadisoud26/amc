enum FieldType {
  email,
  password,
  newPassword,
  birthdayDate,
  gender,
  phoneNumber,
  userName,
  text,
  firstName,
  secondName,
}

enum Evaluation {
  cleanless,
  reseption,
  therapisting,
  treatmentOrMedicalStaff,
}

enum MyState { ok, invalid, another }

enum ImgSource { camera, galery }

enum CalenderDay { mon, sun, sat, wed, tue, thu, fri }

enum TimeState { free, selected, taken }

enum GenderOrRelation { gender, relationShip }

enum WhereIAminNavigationAppBar {
  home,
  addAppoitment,
  notification,
  offer,
  reservedAppointment
}

enum TimeOrDate { date, time }
