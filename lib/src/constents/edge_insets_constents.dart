import 'package:alwadi_medical_center/src/constents/size_int.dart';
import 'package:flutter/material.dart';



const onboardingSlidPadding = EdgeInsets.fromLTRB(20, 20, 20, 0);
const signupAndLoginBodyPadding = EdgeInsets.fromLTRB(30, 0, 30, 30);

const formPadding = EdgeInsets.symmetric(vertical: theFormSize - 10);
const snackBarPadding = EdgeInsets.fromLTRB(10, 10, 0, 0);
const snackBarMargin = EdgeInsets.only(left: 10, right: 10, bottom: 10);
const mainShimmetPadding = EdgeInsets.only(bottom: 8.0);

//---------- drawer edgeInsets -------------
const drawerHeadetMargin = EdgeInsets.only(bottom: 30);
const drawerHeaderAccountPadding = EdgeInsets.only(left: 20 , right: 20);
const drawerListTilePadding = EdgeInsets.fromLTRB(20, 0, 100, 10);
const drawerListTilePaddingForArabic = EdgeInsets.fromLTRB(100, 0, 20, 10);
const drawerListTileContentPadding = EdgeInsets.fromLTRB(10, 0, 0, 0);


const pagesPadding = EdgeInsets.fromLTRB(20, 20, 20, 0);

//------------------ appBar EdgeInsets --------------
const appBarTextPadding = EdgeInsets.fromLTRB(15, 35, 15, 15); // top  : 25
const appBarBackIconPadding = EdgeInsets.only(left: 10, top: 30);
const clinicAppBarBackIconPadding = EdgeInsets.only(top : 20); // top : 10
